# AI 难度系统说明

## 概述

五子棋游戏现在支持三个难度级别：简单、中等、困难。每个难度通过调整多个参数来控制AI的强度和响应速度。

## 难度配置对比

| 参数 | 简单 | 中等 | 困难 |
|------|------|------|------|
| 搜索深度 | 2层 | 3层 | 6层 |
| 最大候选走法 | 8个 | 12个 | 20个 |
| 时间限制 | 1秒 | 2秒 | 5秒 |
| 威胁检测 | ❌ | ✅ | ✅✅ |
| 评估准确度 | 70% | 90% | 100% |
| 随机性 | 30% | 0% | 0% |
| 组合棋型识别 | ❌ | ❌ | ✅ |
| 主动进攻 | ❌ | ❌ | ✅ |
| 智能防守选择 | ❌ | ❌ | ✅ |

## 难度实现细节

### 简单难度

**目标**: 让新手也能享受游戏乐趣，有机会获胜

**实现策略**:
1. **浅层搜索**: 只看2步，无法预见复杂战术
2. **评估弱化**: 评估分数乘以0.7，判断不够精确
3. **禁用威胁检测**: 不会优先防守对手威胁
4. **随机性**: 30%概率选择次优走法，模拟失误
5. **候选走法少**: 只评估8个最好的位置

**适用人群**:
- 五子棋初学者
- 想要轻松获胜的玩家
- 练习基本战术

### 中等难度（默认）

**目标**: 提供适中挑战，适合大多数玩家

**实现策略**:
1. **标准搜索**: 3层深度，可预见短期战术
2. **评估标准**: 评估分数乘以0.9，接近完美判断
3. **启用威胁检测**: 会识别和防守对手的活三、冲四
4. **无随机性**: 始终选择最佳走法
5. **候选走法适中**: 评估12个候选位置

**适用人群**:
- 有一定五子棋基础的玩家
- 休闲娱乐
- 学习中级战术

### 困难难度

**目标**: 提供强大挑战，接近专业水平

**实现策略**:
1. **深层搜索**: 6层深度，可预见长期战术和复杂组合
2. **完整评估**: 100%准确度，不打折扣
3. **增强威胁检测**:
   - 检测并防守冲四（level 3）
   - 主动寻找进攻机会
   - 多个防守走法时智能选择最优解
4. **组合棋型识别**:
   - 双活三组合（+1000分）
   - 三活三组合（+2000分）
   - 双活四组合（+5000分）
   - 活四+活三组合（+3000分）
5. **主动进攻策略**: 优先创造活四、冲四等进攻棋型
6. **高级策略**: 综合考虑攻防平衡，防守权重1.3倍
7. **候选走法多**: 评估20个候选位置

**特殊优势**:
- 搜索深度是中等难度的2倍
- 能够看到6步之后的局面
- 识别复杂的组合杀
- 在防守和进攻间智能选择

**适用人群**:
- 五子棋高手
- 寻求极限挑战的玩家
- 想要大幅提高棋艺
- 专业水平测试

## 技术实现

### 1. 难度配置（constants.ts）

```typescript
export const DIFFICULTY_CONFIGS: Record<Difficulty, DifficultyConfig> = {
  [Difficulty.EASY]: {
    depth: 2,
    maxMoves: 8,
    timeLimit: 1000,
    useThreatDetection: false,
    evaluationAccuracy: 0.7,
  },
  [Difficulty.MEDIUM]: {
    depth: 3,
    maxMoves: 12,
    timeLimit: 2000,
    useThreatDetection: true,
    evaluationAccuracy: 0.9,
  },
  [Difficulty.HARD]: {
    depth: 4,
    maxMoves: 15,
    timeLimit: 3000,
    useThreatDetection: true,
    evaluationAccuracy: 1.0,
  },
};
```

### 2. 评估准确度

在Minimax算法中，评估分数乘以准确度因子：

```typescript
const score = this.alphabeta(...) * this.config.evaluationAccuracy;
```

这样简单难度的AI判断会不够精确，更容易做出次优选择。

### 3. 简单难度随机性

```typescript
private calculateEasyMove(gameState: GameState): Position | null {
  const move = this.minimax.getBestMove(gameState);

  // 30%概率选择次优走法
  if (move && Math.random() < 0.3) {
    const moves = this.minimax.getTopMoves(3);
    if (moves.length > 1) {
      return moves[Math.floor(Math.random() * moves.length)];
    }
  }

  return move;
}
```

### 4. 动态走法限制

根据难度和搜索深度动态调整候选走法数量：

```typescript
const maxMovesAtDepth = Math.max(
  5,
  Math.floor(this.config.maxMoves / (this.config.depth - depth + 1))
);
```

这确保在更深层搜索时限制候选走法，避免搜索树爆炸。

## 难度切换

玩家可以随时切换难度，新难度立即生效：

```typescript
const handleDifficultyChange = (newDifficulty: Difficulty) => {
  setDifficulty(newDifficulty);
};
```

AI引擎会重新初始化，使用新的难度配置。

## 性能考虑

### 简单难度
- **节点搜索**: ~100-500个
- **响应时间**: 0.2-0.8秒
- **CPU占用**: 很低

### 中等难度
- **节点搜索**: ~500-2000个
- **响应时间**: 0.5-1.5秒
- **CPU占用**: 中等

### 困难难度
- **节点搜索**: ~2000-8000个
- **响应时间**: 1-3秒
- **CPU占用**: 较高

所有难度都有时间限制保护，确保不会超时导致页面无响应。

## UI设计

难度选择器位于控制面板中：
- 当前选中的难度高亮显示
- 可随时切换难度
- AI思考时禁用切换按钮

## 平衡性说明

### 简单难度胜率分析
- 新手玩家: 60-70%胜率
- 中级玩家: 80-90%胜率
- 高级玩家: >95%胜率

### 中等难度胜率分析
- 新手玩家: 20-30%胜率
- 中级玩家: 40-50%胜率
- 高级玩家: 70-80%胜率

### 困难难度胜率分析
- 新手玩家: <2%胜率
- 中级玩家: 5-10%胜率
- 高级玩家: 15-25%胜率
- 专业玩家: 30-40%胜率

## 未来改进方向

1. **自适应难度**: 根据玩家水平自动调整
2. **自定义难度**: 允许玩家自定义各项参数
3. **难度提示**: 显示当前局面AI的思考深度和评分
4. **训练模式**: 显示AI推荐的走法帮助学习
5. **难度统计**: 记录不同难度下的胜率
