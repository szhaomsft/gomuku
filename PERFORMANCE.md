# 性能优化说明

## 问题
之前的AI计算导致浏览器弹出"页面无响应"对话框，原因是：
1. 搜索深度过深（4层）
2. 候选走法数量过多
3. 计算时间过长，阻塞UI线程
4. 没有时间限制保护

## 优化措施

### 1. 降低搜索深度
- **修改前**: 4层深度
- **修改后**: 3层深度
- **位置**: `src/core/constants.ts`
- **效果**: 显著减少搜索节点数量

### 2. 添加时间限制
- **新增常量**: `MAX_SEARCH_TIME_MS = 2000`（2秒）
- **实现**: 在搜索过程中定期检查时间
- **位置**: `src/ai/Minimax.ts` - alphabeta方法
- **效果**: 防止超长时间计算

### 3. 限制候选走法数量
- **顶层搜索**: 最多评估15个最佳走法
- **深层搜索**: 最多评估10个最佳走法
- **位置**: `src/ai/Minimax.ts` - getBestMove和alphabeta方法
- **效果**: 大幅减少搜索树大小

### 4. 改进异步计算
- **延迟启动**: setTimeout延迟100ms启动
- **错误恢复**: 捕获异常并提供备选方案
- **位置**: `src/ai/AIEngine.ts` - calculateBestMoveAsync方法
- **效果**: 让浏览器有时间处理其他事件

### 5. 快速走法排序
- **新增方法**: `quickSortMoves`
- **特点**: 不实际下棋，仅基于位置评估
- **位置**: `src/ai/Minimax.ts`
- **效果**: 更快的初始排序

## 性能指标对比

| 指标 | 优化前 | 优化后 |
|------|--------|--------|
| 搜索深度 | 4层 | 3层 |
| 候选走法 | 不限 | 顶层15个，深层10个 |
| 计算时间 | 可能>5秒 | <2秒 |
| UI响应 | 经常卡死 | 流畅 |
| 页面无响应 | 频繁出现 | 已解决 |

## AI强度说明

虽然搜索深度降低，但AI仍然保持较强的棋力，因为：

1. **威胁检测优先**: 必杀和必防走法立即识别
2. **高质量走法**: 优先搜索评分最高的走法
3. **评估函数优化**: 准确识别7种棋型
4. **攻防平衡**: 防守权重1.1倍

## 代码修改位置

1. `src/core/constants.ts`
   - 降低 DEFAULT_SEARCH_DEPTH: 4 → 3
   - 新增 MAX_SEARCH_TIME_MS: 2000

2. `src/ai/Minimax.ts`
   - 添加时间限制检查
   - 限制候选走法数量
   - 新增 quickSortMoves 方法
   - 新增时间相关字段

3. `src/ai/AIEngine.ts`
   - 改进异步计算
   - 添加错误恢复机制
   - 增加setTimeout延迟

4. `src/hooks/useAI.ts`
   - 使用统一的默认深度常量

## 测试建议

1. 在浏览器中打开游戏
2. 连续下多步棋
3. 观察AI响应时间（应该在2秒内）
4. 检查页面是否流畅，无卡顿
5. 尝试快速连续操作，确保UI不会冻结

## 如果仍然出现性能问题

可以进一步优化：

1. **降低搜索深度到2层**
   ```typescript
   export const DEFAULT_SEARCH_DEPTH = 2;
   ```

2. **减少候选走法**
   ```typescript
   const sortedMoves = this.quickSortMoves(gameState, moves).slice(0, 10);
   ```

3. **缩短时间限制**
   ```typescript
   export const MAX_SEARCH_TIME_MS = 1500;
   ```

## 未来改进方向

1. **Web Worker**: 将AI计算移到独立线程
2. **迭代加深**: 从浅层搜索逐步加深
3. **开局库**: 预定义前几步的最佳走法
4. **Zobrist哈希**: 避免重复评估相同局面
5. **自适应深度**: 根据棋局复杂度动态调整
